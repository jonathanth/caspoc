# CASPOC

A package that performs double-split K-fold cross-validation for sPLS with mixOmics.

Under development.

## Installation

You can install the development version from GitHub with:

```r
remotes::install_github("jonathanth/caspoc")
```
